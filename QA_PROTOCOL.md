# 扫描版 OCR 人工核验流程

这份流程用于检查本地扫描版 PDF，不把 OCR 当作规范原文。公开代码仓库只保存流程和检查结论；原始 PDF、OCR 全文、人工誊录的规范正文和核验清单留在被 Git 忽略的本地 `data/`。

## 逐条检查

1. 锁定来源：确认标准名称、版本、发布网站及 PDF 的 SHA-256；记录**PDF 文件页码**，不要与规范印刷页码混用。
2. 打开原始扫描页，先确认候选编号真实存在。三组 OCR 一致也只能说明值得检查，不能自动认定编号正确。
3. 从该编号检查到下一编号，留意条文是否从上一页开始或延续至下一页。逐字核对正文、分项序号、否定词与标点。
4. 单独核对每个数值、单位、比较符号和适用条件。表格按标题、表头、每一行与脚注建立对应关系，不能把 OCR 的顺序文本直接当成表格。
5. 在本地核验清单中填写检查结果和问题。只有编号、正文、数值单位、表格/跨页关系均核实，且出处与使用边界清楚，才可将条文列为可检索候选。公开展示规范内容还需另行确认再发布权利。

运行 `python -m building_code_demo.review_queue --ocr data/full_standard.ocr.json` 会生成本地 `data/review_queue.csv`。每条候选记录的初始状态都是 `pending`；`number_check_needed=yes` 仅表示并非三组 OCR 都识别出该编号，`no` 也不是通过核验。该文件的记录数量不是规范条文总数或准确率。

## 原页抽查记录（2026-09-24）

来源：三明市住建局公开的 GB 55031-2022 扫描版 PDF，SHA-256 `0c792fac5b2524f0326dcb750d89ed90c23cd2768f49dcbdfbd3679ca911bf8e`。以下只记录已检查的局部事实，**没有任何一条完整条文被批准入库**。

| 候选编号 | PDF 页码 | 原图与 OCR 对照 | 结论 |
| --- | ---: | --- | --- |
| `5.2.1` | 14 | 原图中的高度数值为 `0.70m`；一组 OCR 在数值前多出左括号。 | 编号与该数值已定位；完整正文仍待逐字核验。 |
| `5.3.9` | 15 | 原图有参数表、三条数据行和表下注；OCR 输出未可靠保留表头与行列对应。 | 表格结构待人工重建与双重核对，不可直接入库。 |
| `6.5.6` | 22–23 | 条文起始位于第 22 页末，续文位于第 23 页首；OCR 按页拆开。 | 必须核对跨页拼接，当前不能作为完整条文检索。 |
| `6.6.1` | 23 | 原图可定位栏杆高度条款；OCR 版本有字形和标点差异。 | 数值和条件需与原页完整复核。 |

本地清单共 143 条**候选编号记录**，其中 15 条只被部分识别设置检出；它们需要优先核对编号。完整规范其余内容、所有表格和跨页情况尚未做穷尽核验。当前系统的检索仍只针对先前的文字型节选 PDF。
